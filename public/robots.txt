# Allow all well-behaved bots and AI crawlers
User-agent: *
Allow: /

# Internal pages — also marked noindex via per-route meta tags in src/App.jsx
# (INTERNAL_ROUTES). Listing them here adds a second layer for crawlers
# that respect robots.txt but ignore noindex.
Disallow: /audit
Disallow: /todo
Disallow: /layout-demo
Disallow: /blog-editor

Sitemap: https://anthonyships.com/sitemap.xml
